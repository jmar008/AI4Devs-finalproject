# ==============================================
# DealaAI - Producción (EasyPanel)
# ==============================================
# Variables de entorno requeridas para deployment

# Database
DB_NAME=dealaai_prod
DB_USER=postgres
DB_PASSWORD=GENERA_UNA_CLAVE_SEGURA
DB_HOST=db
DB_PORT=5432

# Django
SECRET_KEY=GENERA_UNA_CLAVE_SEGURA
DJANGO_SETTINGS_MODULE=dealaai.settings.production
DEBUG=False

# Hosts y CORS
ALLOWED_HOSTS=mcp.jorgemg.es,backend,localhost
CORS_ALLOWED_ORIGINS=https://mcp.jorgemg.es

# API Keys (Opcional)
OPENAI_API_KEY=sk-proj-...

# pgAdmin (Opcional)
PGADMIN_PASSWORD=admin

# Email (Opcional - Si no se configura, los emails se imprimen en consola)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu-email@gmail.com
# EMAIL_HOST_PASSWORD=tu-password-de-aplicacion
# DEFAULT_FROM_EMAIL=noreply@mcp.jorgemg.es

# ==============================================
# Cómo generar claves seguras:
# ==============================================
# python3 -c "import secrets; print(secrets.token_urlsafe(50))"
